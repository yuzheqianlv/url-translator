# 本地开发环境配置示例
# 复制此文件为 .env.local 并修改相应值
# .env.local 文件会被 .gitignore 忽略，适合存放本地特定配置

# ===== 本地开发覆盖配置 =====
# 这些配置会覆盖 .env 中的设置

# 本地后端API地址（如果后端运行在不同端口）
FRONTEND_API_BASE_URL=http://localhost:3002/api/v1

# 本地开发特定设置
DEBUG_MODE=true
RUST_LOG=debug
WASM_LOG=debug

# 如果需要使用不同的主题进行测试
DEFAULT_THEME=mocha

# 本地功能测试开关
ENABLE_PROJECT_MANAGEMENT=true
ENABLE_HISTORY=true
ENABLE_SEARCH=true
ENABLE_BATCH_TRANSLATION=true

# 本地性能设置
FRONTEND_API_TIMEOUT_SECONDS=60

# 开发工具
ENABLE_DEVTOOLS=true
ENABLE_PERFORMANCE_MONITORING=true