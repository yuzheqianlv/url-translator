# =============================================================================
# URL Translator Project - .gitignore
# =============================================================================

# Rust 相关文件
# =============================================================================
/target/                    # Rust 构建输出目录
**/*.rs.bk                  # rustfmt 备份文件
*.pdb                       # Windows 调试符号

# Trunk 和 WASM 相关文件
# =============================================================================
/dist/                      # Trunk 构建输出目录
*.wasm                      # WASM 二进制文件（除了源代码中的）
.trunk/                     # Trunk 配置缓存
pkg/                        # wasm-pack 输出目录

# 开发工具和测试文件
# =============================================================================
lcov.info                   # 代码覆盖率报告
*.profraw                   # 性能分析文件
*.profdata                  # 性能分析数据
coverage/                   # 覆盖率报告目录
test-results/               # 测试结果目录
criterion/                  # 性能测试结果

# IDE 和编辑器文件
# =============================================================================
.vscode/                    # VS Code 配置
.idea/                      # IntelliJ IDEA 配置
*.swp                       # Vim 交换文件
*.swo                       # Vim 临时文件
*~                          # 备份文件
.fleet/                     # JetBrains Fleet 配置
.helix/                     # Helix 编辑器配置

# 操作系统文件
# =============================================================================
.DS_Store                   # macOS Finder 信息
.DS_Store?                  # macOS 替代文件
._*                         # macOS 资源分支
.Spotlight-V100             # macOS Spotlight 索引
.Trashes                    # macOS 垃圾箱
ehthumbs.db                 # Windows 缩略图缓存
Thumbs.db                   # Windows 缩略图数据库
Desktop.ini                 # Windows 桌面配置

# 临时文件和日志
# =============================================================================
*.tmp                       # 临时文件
*.temp                      # 临时文件
*.log                       # 日志文件
*.err                       # 错误日志
debug.log                   # 调试日志
npm-debug.log*              # NPM 调试日志
yarn-debug.log*             # Yarn 调试日志
yarn-error.log*             # Yarn 错误日志

# 环境变量和配置文件
# =============================================================================
.env                        # 环境变量文件
.env.local                  # 本地环境变量
.env.*.local                # 特定环境的本地变量
*.env                       # 所有环境文件

# Docker 和部署文件
# =============================================================================
docker-compose.override.yml # 本地 Docker 配置覆盖
.dockerignore               # Docker 忽略文件（如果动态生成）

# Node.js 相关（如果使用前端构建工具）
# =============================================================================
node_modules/               # Node.js 依赖
npm-debug.log               # NPM 调试日志
package-lock.json           # NPM 锁定文件（可根据项目需要调整）
yarn.lock                   # Yarn 锁定文件

# 版本控制相关
# =============================================================================
*.orig                      # 合并冲突原始文件
*.rej                       # 补丁拒绝文件
*.bak                       # 备份文件
*.backup                    # 备份文件
*.old                       # 旧版本文件

# 其他生成文件
# =============================================================================
*.pid                       # 进程ID文件
*.seed                      # 随机种子文件
*.pid.lock                  # 进程锁文件
.cache/                     # 缓存目录
.nyc_output/                # NYC 覆盖率工具输出
.sass-cache/                # Sass 缓存

# 项目特定忽略
# =============================================================================
# 调试和测试文件
test_debug.rs               # 调试测试文件
debug_*.rs                  # 调试源文件
*_debug.rs                  # 调试源文件

# 可选的项目特定忽略项
# 添加项目特定的忽略项
# 例如：
# /translations/              # 翻译文件目录（如果是临时的）
# /downloads/                 # 下载文件目录
# /uploads/                   # 上传文件目录
# Rust 编译产物
target/
backend/target/
Cargo.lock

# 其他常见忽略文件
.DS_Store
*.log
node_modules/
target/
backend/target/
